---
layout: layouts/home.njk
eleventyNavigation:
  key: Ook Rahmawan
  order: 1
---
{% set maxPosts = collections.posts.length | min(999) %}
<h1>Ook Rahmawan {% if maxPosts == 1 %}Post{% else %}({{ maxPosts }}){% endif %}</h1>

{% set postslist = collections.posts | head(-999) %}
{% set postslistCounter = collections.posts | length %}
{% include "postslist.njk" %}

<p>More posts can be found in <a href="{{ '/posts/' | url }}">the archive</a>.</p>
